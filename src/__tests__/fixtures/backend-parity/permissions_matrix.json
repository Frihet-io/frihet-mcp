{
  "tool": "permissions_matrix",
  "provenance": "erp-main/functions/src/publicApi/families/permissions.ts:32-45 getMatrix → { statusCode: 200, result: { data: { roles, resources, actions, legacyAliases, matrix, source } } }. `roles` is RBAC_ROLES — a STRING array (families/core/permissionMatrix.ts:18), never objects with a `permissions` field. There is no generatedAt. Envelope: publicApi.ts:2508-2516 → { data: {...}, meta }.",
  "declaredKeysNotInFixtures": {},
  "cases": [
    {
      "name": "live backend body — RBAC matrix mirroring firestore.rules roleAllows()",
      "args": {},
      "routes": {
        "GET /permissions/matrix": {
          "data": {
            "roles": ["owner", "admin", "manager", "sales", "accountant", "employee", "viewer"],
            "resources": [
              "workspace", "invoices", "quotes", "expenses", "clients", "products", "accounting",
              "people", "payroll", "integrations", "banking", "settings", "audit_log", "billing"
            ],
            "actions": ["read", "create", "update", "delete", "bulk_delete", "export"],
            "legacyAliases": { "editor": "sales" },
            "matrix": {
              "owner": { "workspace": ["read", "update", "export"], "billing": ["read", "create", "update", "delete", "export"] },
              "viewer": { "workspace": ["read"], "invoices": ["read"] }
            },
            "source": "firestore.rules roleAllows()"
          },
          "meta": { "requestId": "req_parity_23", "timestamp": "2026-08-08T10:00:22.000Z" }
        }
      }
    }
  ]
}
