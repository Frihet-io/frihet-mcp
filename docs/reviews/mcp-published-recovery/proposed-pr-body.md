Sister-repository MCP references currently follow the package being prepared in this repository, which can advertise a version that users cannot install. This change keeps the self-audit on the repository contract and checks sister repositories against npm metadata plus the integrity-verified package tarball. Canonical registration names come from parsed `dist/tools/*.js`, without importing package code or running install hooks. Missing or unverifiable artifact bytes return `INCONCLUSIVE` (exit 4) before any rewrite.

Rewrites apply only to the flagged line. Tests cover safe-line preservation, version/count separation, corrupted archives, identity mismatches, unsafe/duplicate paths, dynamic registrations and misleading comments/strings. Synthetic registry responses make the suite independent of network and local release tags. The earlier `fetch-tags: true` change did not repair the CI failure; it has been removed and `ci.yml` is restored byte-for-byte to main. The version environment variable now requests that exact npm version and still requires registry access.

Validation on the recovery worktree: root build and 1036 tests passed; Worker typecheck and 112 tests passed; 69 targeted audit tests passed; conformance, OpenAI descriptor/submission/schema, onboarding and public-capability gates passed. npm 1.17.0 was downloaded with matching sha512 integrity and contains 157 canonical static registrations; all 57 shipped JS files also match a fresh rebuild of its release tag. Hash review and per-file evidence are in `docs/reviews/mcp-published-recovery/`.

This does **not** resolve the separate `Published artifact drift` release debt: main prepares 1.18.0 while npm and the MCP Registry serve 1.17.0, a failure observed before #181. The Full OAuth release hold remains in force. No `--fix` was run against a real repository. Remote CI must be assessed on the final pushed head; local results are not a claim of remote success.


The review follow-up also rejects canonical registration references in unsupported files or syntactic forms, verifies transport and archive bounds, deduplicates repeated findings on the same line, and makes symlinked CLI entrypoints run the audit. Goldens reproduce 8/8 tagless failures, 5/5 partial-count/rewrite failures and the aliased-entrypoint false success before their respective fixes. All pass after correction. These use synthetic fixtures; the npm archive measurement remains separate.

The artifact check is static syntax analysis, not execution or a proof of arbitrary JavaScript behavior, runtime permissions or Worker availability. Existing interception adapters are limited to four named modules and known shapes; unsupported recognized registration forms fail closed. Final external review and remote CI must assess the committed head.
